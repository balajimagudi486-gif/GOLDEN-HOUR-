{
  "name": "goldenhour",
  "version": "1.1.0",
  "private": true,
  "description": "GoldenHour - Emergency Ambulance Dispatch & Hospital Pre-Arrival Platform",
  "scripts": {
    "build": "npm run build --prefix client",
    "install:all": "pip install -r server/requirements.txt && cd client && npm install",
    "server": "cd server && python -m uvicorn main:app --host 0.0.0.0 --port 4000",
    "client": "cd client && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}